# Shared hooks live in @rm3/lefthook-config. Run `pnpm lefthook install` after `pnpm install`.
extends:
  - node_modules/@rm3/lefthook-config/lefthook.yml
# Repo-specific jobs go here. Never reuse the names oxfmt / oxlint / no-focused-tests:
# extended files override same-named local jobs.
